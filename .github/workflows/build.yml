on: push
jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: .
    name: build
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v2
        with:
          distribution: adopt
          java-version: 17
          cache: maven
      - run: mvn clean install